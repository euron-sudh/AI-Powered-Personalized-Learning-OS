**Last Updated:** 2026-04-17 16:00

# 🎓 LearnOS — AI-Powered Personalized Learning Platform

An intelligent tutoring system that adapts to each student in real time using:
- **🎙️ Voice Chat** — Speak naturally with an AI tutor
- **📚 Smart Lessons** — AI generates personalized curriculum
- **😊 Emotion Detection** — Camera analyzes student engagement and adapts
- **🎯 Adaptive Learning** — Content difficulty adjusts based on performance

## ⚡ Quick Links

- **[Getting Started Guide](GETTING_STARTED.md)** ← **Start here if you're new!**
- [Full Documentation](docs/CLAUDE.md)
- [Troubleshooting](GETTING_STARTED.md#-troubleshooting)
- [Project Status](docs/CHANGELOG.md)

---

## 🚀 Start in 2 Minutes

```bash
# Terminal 1: Backend
cd backend && python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
alembic upgrade head
python -m uvicorn app.main:app --port 8000

# Terminal 2: Frontend  
cd frontend && npm install && npm run dev
```

Visit: **http://localhost:3001**

---

## 📋 System Requirements

- Python 3.11+
- Node.js 18+
- Supabase Account (free tier works)
- OpenAI API Key
- Anthropic API Key

---

## ✨ Features

### Authentication
- Email/password registration and login via Supabase Auth
- **Google OAuth** sign-in on both login and register pages
- **Forgot password** flow — email reset link + set new password page with strength indicator
- Show/hide password toggle on login and reset pages
- JWT-authenticated FastAPI backend with row-level security on all data

### Onboarding
- Multi-step onboarding wizard: grade, curriculum board, subjects, background, marksheet upload
- Board selection: **CBSE, ICSE, Cambridge IGCSE, IB, Common Core**
- Marksheet uploaded to private Supabase Storage bucket

### Dashboard
- Subject cards with progress, chapter counts, and average scores
- **Clickable stat cards** — Subjects navigates to `/learn`, Chapters Done / Average Score scroll to Progress Overview, Total Chapters scrolls to Continue Learning
- Hero section with resume lesson and AI tutor shortcuts
- Progress Overview with per-subject progress bars
- Live updates via **Supabase Realtime**

### AI Curriculum Generation
- Personalized curriculum generated by **Claude Opus 4** per subject and board
- Official syllabus data for **CBSE, ICSE, Cambridge IGCSE, IB, and Common Core**
- Board-specific content style: NCERT format for CBSE, analytical depth for ICSE, command-word style for Cambridge, inquiry-based for IB, CCSS-aligned for Common Core
- 8–12 ordered chapters per subject with titles, descriptions, and learning objectives

### AI Lesson Content
- Rich HTML lesson content generated per chapter (600–900 words)
- **Mermaid.js diagrams** rendered live in-browser
- **LaTeX formulas** rendered via KaTeX
- Key concepts, real-world examples, and chapter summary

### AI Teaching Chat
- Streaming conversational tutor powered by **Claude Opus 4** (Server-Sent Events)
- Socratic teaching method — guides students to answers rather than giving them
- Full conversation history persisted per chapter
- Adaptive explanations based on student responses
- Sentiment-triggered tutor nudges (e.g. "Take a break" when drowsy)

### Activities & Evaluation
- AI-generated activities per chapter: multiple choice, short answer, problem sets
- Board-aligned question style (NCERT format, Cambridge command words, etc.)
- **Claude Opus 4** evaluates responses with: score (0–100), correctness breakdown, detailed feedback, and guidance
- Strength/weakness tracking updates the student learning profile automatically
- Next chapter unlocked on completion

### Adaptive Curriculum Adjustment
- After a score below 60%, **Claude Opus 4** automatically re-orders remaining chapters
- Chapters addressing identified weak topics are promoted earlier in the sequence
- Prerequisites are preserved; re-ordering is transparent to the student

### Voice Tutoring (Speech-to-Speech)
- Real-time bidirectional audio via **OpenAI Realtime API** (WebSocket)
- Server-side Voice Activity Detection (VAD) with 800 ms silence threshold
- Natural interruption — student speech cancels in-progress AI response
- Full conversation transcript (student + AI) with download button
- Lesson-context-aware: AI tutor is briefed on the current chapter title
- Available in-lesson (Voice tab) and as a standalone page (`/voice`)

### Video Sentiment Analysis
- Live webcam feed via WebRTC (no video stored)
- Frames captured every 5 seconds, analyzed by **Claude Vision**
- Detects 6 emotions: **engaged, happy, confused, bored, frustrated, drowsy**
- Results streamed back in real time via **WebSocket** (`/api/video/sentiment/ws`)
- Adaptive actions surfaced in the lesson chat
- All raw frames discarded immediately after classification

### Courses
- Dedicated **Courses page** (`/courses`) with curated external course recommendations
- Grouped by the student's enrolled subjects with subject filter tabs
- Platforms: Khan Academy, Coursera, edX (MIT/Harvard/Stanford), YouTube (3Blue1Brown, Crash Course, etc.)
- Covers Mathematics, Physics, Chemistry, Biology, English, Computer Science, History, Geography, Economics

### Analytics Dashboard
- **Bar chart**: average score per subject (color-coded green/yellow/red)
- **Stacked area chart**: emotion timeline across sessions (30-minute buckets)
- **Donut chart**: emotion distribution across all readings
- Chapter progress bars per subject
- Learning profile: AI-identified strengths and areas for improvement
- **Live updates via Supabase Realtime**

### Navigation (Session-First)
- **Home** → Dashboard overview, today's learning plan, AI coach insights
- **Session** → Unified learning experience: lesson + chat + voice + activities (no duplicate "Learn" + "AI Tutor")
- **Path** → Learning roadmap: subjects, chapters, progress visualization
- **Insights** → Analytics dashboard: performance trends, emotional journey, strengths/weaknesses
- Profile dropdown: avatar, email, Profile Details, Learning Path, Start Learning, Preferences, Sign Out

### Today's Focus (AI Priority Engine)
- Smart dashboard section showing **ONE recommended topic** picked by AI
- Priority scoring: performance (low score → higher priority) + sentiment (confusion/frustration boost) + time decay (inactive topics resurface)
- Shows: topic name, reason ("You struggled here 48%" or "You're improving"), duration, "Start Now" button
- Secondary items: next 2 topics in collapsed list
- Powered by `/api/progress/today-focus` endpoint combining score + sentiment + weakness data

---

## Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        CLIENT (Next.js 14+)                        │
│  Dashboard │ Lesson Viewer │ Voice Chat │ Video Sentiment Feed      │
└────────────────────────────┬────────────────────────────────────────┘
                             │ HTTPS / WebSocket / WebRTC
┌────────────────────────────▼────────────────────────────────────────┐
│                  BACKEND API (Python / FastAPI)                      │
│      Supabase Auth │ Rate Limiting │ WebSocket Manager               │
└──┬──────────┬──────────┬──────────┬──────────┬───────────┬──────────┘
   │          │          │          │          │           │
   ▼          ▼          ▼          ▼          ▼           ▼
Onboarding Curriculum Teaching   Voice    Sentiment  Activities
  Router    Generator  Engine    Engine   Analyzer   Evaluator
                             │
          ┌──────────────────┼──────────────────┐
          ▼                  ▼                  ▼
    Claude API         OpenAI Realtime     Claude Vision
  (curriculum,         (speech-to-speech   (frame sentiment
   teaching, eval,      WebSocket)          analysis)
   adjustment)
          │
          ▼
    Supabase (PostgreSQL + Auth + Storage + Realtime)
```

---

## Tech Stack

| Layer | Technology |
|---|---|
| Frontend | Next.js 14 (App Router), React 18, TypeScript, Tailwind CSS |
| Charts | Recharts |
| Diagrams | Mermaid.js |
| Formulas | KaTeX |
| Backend | Python 3.11+, FastAPI, Uvicorn, Pydantic v2 |
| AI Curriculum & Teaching | Anthropic Claude Opus 4 (anthropic SDK) |
| AI Voice | OpenAI Realtime API (WebSocket, server VAD) |
| AI Sentiment | Claude Vision API |
| Auth | Supabase Auth (email/password, Google OAuth, JWT verification) |
| Database | Supabase PostgreSQL (SQLAlchemy async + asyncpg) |
| Realtime | Supabase Realtime (live sentiment dashboard) |
| File Storage | Supabase Storage (marksheets, diagrams) |
| Migrations | Alembic |
| Cache | Redis (rate limiting, optional session cache) |
| Infrastructure | Docker Compose (Redis locally; Supabase cloud) |

---

## Project Structure

```
project4/
├── backend/
│   ├── app/
│   │   ├── main.py                    # FastAPI app, CORS, lifespan
│   │   ├── config.py                  # Pydantic Settings (env vars)
│   │   ├── dependencies.py            # JWT auth dependency
│   │   ├── routers/
│   │   │   ├── auth.py                # JWT verify
│   │   │   ├── onboarding.py          # Student profile + marksheet upload
│   │   │   ├── curriculum.py          # Generate, fetch, and adjust curriculum
│   │   │   ├── lessons.py             # Lesson content + streaming chat
│   │   │   ├── voice.py               # OpenAI Realtime WebSocket proxy
│   │   │   ├── video.py               # Sentiment HTTP + WebSocket endpoints
│   │   │   ├── activities.py          # Activity fetch, submit, evaluate
│   │   │   ├── progress.py            # Student progress & analytics
│   │   │   └── notes.py               # Per-chapter notes CRUD
│   │   ├── services/
│   │   │   ├── curriculum_generator.py # Curriculum, content, activity, adjustment
│   │   │   ├── teaching_engine.py     # Streaming Socratic tutor
│   │   │   ├── activity_evaluator.py  # AI grading & feedback
│   │   │   ├── sentiment_analyzer.py  # Claude Vision frame analysis
│   │   │   ├── voice_manager.py       # OpenAI Realtime session creation
│   │   │   └── syllabus_data.py       # Official board syllabus data
│   │   ├── models/                    # SQLAlchemy ORM models
│   │   ├── schemas/                   # Pydantic request/response schemas
│   │   └── core/
│   │       ├── ai_client.py           # Anthropic Claude client wrapper
│   │       ├── database.py            # Async SQLAlchemy engine & session
│   │       ├── security.py            # Supabase JWT verification
│   │       ├── supabase_client.py     # Supabase admin client
│   │       └── rate_limiter.py        # slowapi rate limiting
│   ├── alembic/                       # DB migration files
│   └── requirements.txt
│
├── frontend/
│   ├── src/
│   │   ├── app/
│   │   │   ├── (auth)/login/          # Login page (email + Google OAuth + forgot password)
│   │   │   ├── (auth)/register/       # Registration page (email + Google OAuth)
│   │   │   ├── auth/callback/         # OAuth + password-reset callback handler
│   │   │   ├── auth/reset-password/   # Set new password page
│   │   │   ├── forgot-password/       # Request password reset email
│   │   │   ├── onboarding/            # 5-step onboarding wizard
│   │   │   ├── dashboard/             # Subject cards + progress + stat cards
│   │   │   ├── learn/
│   │   │   │   ├── [subjectId]/       # Chapter list
│   │   │   │   └── [subjectId]/[chapterId]/
│   │   │   │       ├── page.tsx       # Lesson: content/chat/voice/notes tabs
│   │   │   │       └── activity/      # Chapter quiz
│   │   │   ├── courses/               # External course recommendations
│   │   │   ├── profile/               # Profile details page
│   │   │   ├── analytics/             # Charts + Realtime dashboard
│   │   │   ├── tutor/                 # Standalone AI chat
│   │   │   ├── voice/                 # Standalone voice tutor
│   │   │   └── video-session/         # YouTube cards + live sentiment
│   │   ├── hooks/
│   │   │   ├── useSupabaseAuth.ts     # Auth state management
│   │   │   ├── useVoiceChat.ts        # OpenAI Realtime voice (WebSocket)
│   │   │   ├── useVideoFeed.ts        # WebRTC camera + frame capture
│   │   │   └── useSentiment.ts        # Live sentiment WebSocket client
│   │   ├── components/
│   │   │   ├── Nav.tsx                # Navbar with profile dropdown
│   │   │   ├── SentimentIndicator.tsx
│   │   │   └── ProgressBar.tsx
│   │   └── lib/
│   │       ├── api.ts                 # Fetch wrapper with session caching
│   │       └── supabase.ts            # Browser Supabase client
│   └── package.json
│
└── docker-compose.yml                 # Redis only (Supabase runs cloud)
```

---

## Getting Started

### Prerequisites

- Node.js 20+
- Python 3.11+
- Docker Desktop (for Redis)
- A [Supabase](https://supabase.com) project
- An [Anthropic](https://console.anthropic.com) API key
- An [OpenAI](https://platform.openai.com) API key (Realtime API access required)

### 1. Clone and configure

```bash
git clone <repo-url>
cd project4
```

Copy and fill in the environment files (see [Environment Variables](#environment-variables)):

```bash
cp backend/.env.example backend/.env
cp frontend/.env.local.example frontend/.env.local
```

### 2. Start Redis

```bash
docker-compose up -d
```

### 3. Backend setup

```bash
cd backend
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate
pip install -r requirements.txt
alembic upgrade head            # run migrations against Supabase PostgreSQL
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 4. Frontend setup

```bash
cd frontend
npm install
npm run dev                     # http://localhost:3000
```

### 5. Supabase setup

1. Create a project at [supabase.com](https://supabase.com)
2. Copy **Project URL**, **anon key**, **service role key**, and **JWT secret** into your `.env` files
3. Create storage buckets: `marksheets` and `content`
4. Enable Realtime for the `sentiment_logs` table (Database → Replication → Tables)
5. RLS policies are applied automatically via Alembic migrations

### 6. Google OAuth setup (optional)

1. Go to [Google Cloud Console](https://console.cloud.google.com) → APIs & Services → Credentials → Create OAuth 2.0 Client ID
2. Add authorized redirect URI: `https://<project-ref>.supabase.co/auth/v1/callback`
3. In Supabase Dashboard → Authentication → Providers → Google → paste Client ID and Secret
4. In Supabase → Authentication → URL Configuration → Redirect URLs, add:
   - `http://localhost:3000/auth/callback`
   - `http://localhost:3000/auth/reset-password`

---

## Environment Variables

### Backend (`backend/.env`)

```env
# Supabase
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<anon-key>
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
SUPABASE_JWT_SECRET=<jwt-secret>
SUPABASE_DB_URL=postgresql+asyncpg://postgres:<password>@db.<ref>.supabase.co:5432/postgres

# AI
ANTHROPIC_API_KEY=<claude-api-key>
OPENAI_API_KEY=<openai-api-key>

# Redis
REDIS_URL=redis://localhost:6379

# Server
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["http://localhost:3001","http://localhost:3000"]

# Sentiment
SENTIMENT_FRAME_INTERVAL_MS=5000
SENTIMENT_CONFIDENCE_THRESHOLD=0.6
```

### Frontend (`frontend/.env.local`)

```env
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
NEXT_PUBLIC_API_URL=http://localhost:8000
```

---

## Database Schema

All tables managed via Alembic migrations. Supabase Auth handles `auth.users`.

| Table | Purpose |
|---|---|
| `students` | Profile: grade, board, interests, background, marksheet path |
| `subjects` | Per-student subjects with status and difficulty level |
| `chapters` | Ordered chapters with AI-generated `content_json` |
| `activities` | AI-generated quiz/problem sets per chapter |
| `activity_submissions` | Student responses with `evaluation_json` and score |
| `chat_messages` | Full lesson chat history (student + tutor roles) |
| `sentiment_logs` | Emotion, confidence, action_taken per frame analysis |
| `student_progress` | Aggregated progress: chapters done, scores, strengths, weaknesses |
| `student_notes` | Per-chapter freeform notes (auto-saved) |

---

## API Reference

### Auth
| Method | Endpoint | Description |
|---|---|---|
| POST | `/api/auth/verify` | Verify Supabase JWT, return user info |

### Onboarding
| Method | Endpoint | Description |
|---|---|---|
| POST | `/api/onboarding` | Save student profile, seed subjects |
| POST | `/api/onboarding/marksheet` | Upload marksheet to Supabase Storage |
| GET | `/api/onboarding/profile` | Fetch student profile |

### Curriculum
| Method | Endpoint | Description |
|---|---|---|
| POST | `/api/curriculum/generate` | Generate full curriculum via Claude |
| GET | `/api/curriculum/{subject_id}` | Get curriculum (chapter list) |
| POST | `/api/curriculum/{subject_id}/adjust` | Re-order chapters based on performance |

### Lessons
| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/lessons/{chapter_id}/content` | Get or generate chapter content |
| POST | `/api/lessons/{chapter_id}/chat` | Streaming teaching chat (SSE) |
| GET | `/api/lessons/{chapter_id}/history` | Fetch chat history |

### Voice
| Method | Endpoint | Description |
|---|---|---|
| POST | `/api/voice/session` | Create OpenAI Realtime ephemeral token |
| WS | `/api/voice/ws` | Bidirectional proxy to OpenAI Realtime API |

### Video & Sentiment
| Method | Endpoint | Description |
|---|---|---|
| POST | `/api/video/analyze` | Analyze single frame (HTTP, rate-limited 60/min) |
| WS | `/api/video/sentiment/ws?token=<jwt>` | Live sentiment WebSocket stream |
| GET | `/api/video/sentiment/history` | Fetch recent sentiment log entries |

### Activities
| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/activities/{chapter_id}/activity` | Get chapter activity |
| POST | `/api/activities/{activity_id}/submit` | Submit student response |
| POST | `/api/activities/{activity_id}/evaluate` | AI-evaluate submission |

### Progress & Notes
| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/progress/{student_id}` | Student progress and analytics data |
| GET | `/api/notes/{chapter_id}` | Get chapter notes |
| PUT | `/api/notes/{chapter_id}` | Save chapter notes |

---

## Pages & Routes

| Route | Description |
|---|---|
| `/` | Landing page |
| `/login` | Sign in (email + Google OAuth + forgot password link) |
| `/register` | Sign up (email + Google OAuth) |
| `/forgot-password` | Request password reset email |
| `/auth/callback` | OAuth + password-reset redirect handler |
| `/auth/reset-password` | Set new password after email reset |
| `/onboarding` | 5-step onboarding wizard |
| `/dashboard` | Subject cards, stat cards, progress overview |
| `/learn` | All subjects hub |
| `/learn/[subjectId]` | Chapter list for a subject |
| `/learn/[subjectId]/[chapterId]` | Lesson: content / chat / voice / notes tabs |
| `/learn/[subjectId]/[chapterId]/activity` | Chapter quiz with AI evaluation |
| `/courses` | Curated external course recommendations by subject |
| `/profile` | Account details and learning profile |
| `/tutor` | Standalone AI chat tutor |
| `/voice` | Standalone voice tutor |
| `/video-session` | YouTube chapter videos + live sentiment |
| `/analytics` | Charts, timeline, emotion distribution, learning profile |

---

## AI Integration

### Claude Opus 4 (Anthropic)

| Use case | Approach |
|---|---|
| Curriculum generation | Board-specific guidelines + official syllabus data |
| Chapter content | Rich HTML + Mermaid diagrams + LaTeX formulas, 600–900 words |
| Activity generation | Board-aligned question styles, 4–6 mixed questions per chapter |
| Teaching chat | Socratic method, streaming SSE, full chapter context |
| Activity evaluation | Score + correctness + detailed feedback + strengths/weaknesses |
| Adaptive adjustment | Re-orders remaining chapters by weak topics, preserves prerequisites |
| Sentiment analysis | Claude Vision classifies JPEG frames into 6 emotions with confidence |

### OpenAI Realtime API

- Model: `gpt-4o-realtime-preview`
- Modalities: audio + text
- Voice: `alloy`
- Turn detection: `server_vad`, threshold 0.5, 800 ms silence duration
- Transcription: `whisper-1` for student speech
- FastAPI acts as a transparent bidirectional WebSocket proxy

---

## Deployment

### Backend

Any Python ASGI host (Railway, Render, Fly.io, etc.):

```bash
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### Frontend

Deploy to **Vercel** by connecting the repo, setting the root directory to `frontend/`, and adding the `NEXT_PUBLIC_*` environment variables.

### Redis

Use a managed Redis provider (Redis Cloud, Upstash) in production and update `REDIS_URL`.

---

## Privacy & Security

- **No video stored**: webcam frames are analyzed and immediately discarded; only emotion labels are persisted
- **Row-Level Security**: all Supabase tables are scoped to the authenticated student
- **JWT verification**: every FastAPI route validates the Supabase JWT before processing
- **Rate limiting**: sentiment analysis (60/min) and curriculum generation (10/min) via slowapi
- **COPPA considerations**: no student PII sent to AI models; marksheets in private Supabase Storage bucket with signed URLs
- **OAuth**: Google sign-in handled entirely through Supabase Auth — no OAuth tokens stored in the app
